画像編集タスク(テキスト差し替えのみ・厳守)。

【入力】
@img1 = 現在確定しているサムネイル(ダークグリーン基調、最上部に小ラベル「LP修正・改善」、その下に小見出し「今あるLPを」、特大見出し「もっと良くする」、左下バッジ「修正回数無制限」、右側の女性モデル、下部のスマホモックアップ3台)

【変更する要素(これだけ)】
・小見出しのテキストを「今あるLPを」→「今あるLPをもっと良くする」に変更する。改行させず、必ず一行で収める。文字数が増えるため、フォントサイズを一行に収まる範囲で調整してよいが、現状の小見出しと同じ書体・トーン・配置基準(画面上部・控えめなサイズ)を維持する。
・特大見出し(画面内で最大サイズの袋文字)のテキストを「もっと良くする」→「LP修正・改善」に変更する。文字数が変わるため、現状と同じ書体・装飾(グリーン系グラデーションまたは白抜き+グリーンの縁取り)を保ったまま、画面内に収まるようバランスよく改行・サイズ調整してよい(例:1行目「LP修正」2行目「・改善」、または適切な2行構成)。ただし特大見出しとしての圧倒的な存在感・視認性は維持する。

【絶対に変更してはいけない要素(@img1をそのまま維持)】
・最上部の小ラベル「LP修正・改善」:文言・デザイン・位置そのまま。
・左下バッジ「修正回数無制限」:文言・デザイン・位置そのまま。
・右側の女性モデルの顔・ポーズ・表情・髪型・服装・位置・大きさ。
・背景のコラージュ、配色、下部のスマホモックアップ3台の内容・配置・影。
・全体の構図・余白・縦横比(約6:5)。

【出力条件】
・小見出しは「今あるLPをもっと良くする」の一行のみ(改行禁止)。
・特大見出しは「LP修正・改善」の文言のみ(それ以外の文言を混ぜない)。
・変更後も小さいサムネイル表示ではっきり視認できる可読性を保つ。
・それ以外の要素はすべて@img1と完全に同一に見えること。
・高解像度、ココナラのサービスサムネイルとしてそのまま使える仕上がり。
