const menuButton=document.querySelector('.menu-button');const mobileMenu=document.querySelector('.mobile-menu');menuButton?.addEventListener('click',()=>{const open=menuButton.getAttribute('aria-expanded')==='true';menuButton.setAttribute('aria-expanded',String(!open));menuButton.setAttribute('aria-label',open?'メニューを開く':'メニューを閉じる');mobileMenu.hidden=open});mobileMenu?.querySelectorAll('a').forEach(a=>a.addEventListener('click',()=>{mobileMenu.hidden=true;menuButton.setAttribute('aria-expanded','false')}));
const observer=new IntersectionObserver(entries=>entries.forEach(entry=>{if(entry.isIntersecting){entry.target.classList.add('is-visible');observer.unobserve(entry.target)}}),{threshold:.12});document.querySelectorAll('.reveal').forEach(el=>observer.observe(el));
const track=document.querySelector('.slider__track');const cards=[...document.querySelectorAll('.product-card')];const dots=[...document.querySelectorAll('.slider__dots button')];let slide=0;function mobile(){return matchMedia('(max-width:767px)').matches}function setSlide(next){if(!mobile()){track.style.transform='';return}slide=(next+cards.length)%cards.length;track.style.transform=`translateX(calc(-${slide*100}% - ${slide*24}px))`;dots.forEach((dot,i)=>dot.classList.toggle('is-active',i===slide))}document.querySelector('.slider-button--next')?.addEventListener('click',()=>setSlide(slide+1));document.querySelector('.slider-button--prev')?.addEventListener('click',()=>setSlide(slide-1));dots.forEach(dot=>dot.addEventListener('click',()=>setSlide(Number(dot.dataset.slide))));addEventListener('resize',()=>setSlide(slide));let startX=0;track?.addEventListener('touchstart',e=>startX=e.touches[0].clientX,{passive:true});track?.addEventListener('touchend',e=>{const d=e.changedTouches[0].clientX-startX;if(Math.abs(d)>45)setSlide(slide+(d<0?1:-1))},{passive:true});
const toast=document.querySelector('.toast');let toastTimer;document.querySelectorAll('[data-toast]').forEach(btn=>btn.addEventListener('click',()=>{toast.textContent=btn.dataset.toast;toast.classList.add('is-visible');clearTimeout(toastTimer);toastTimer=setTimeout(()=>toast.classList.remove('is-visible'),2200)}));
const modal=document.querySelector('.entry-modal');document.querySelector('[data-open-modal]')?.addEventListener('click',()=>modal.showModal());document.querySelector('.modal-close')?.addEventListener('click',()=>modal.close());modal?.addEventListener('click',e=>{if(e.target===modal)modal.close()});modal?.querySelector('form')?.addEventListener('submit',()=>{setTimeout(()=>{toast.textContent='デモのため送信は行いません';toast.classList.add('is-visible');setTimeout(()=>toast.classList.remove('is-visible'),2500)},100)});
